spring:
  application:
    name: supermercado

  datasource:
    url: jdbc:mysql://localhost:3306/supermarket?useSSL=false&serverTimezone=UTC
    username: root
    password: aqui_tu_contraseña_de_mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080